import AnimateNavigation from "@/components/ui/AnimateNavigation";

export default function Template({ children }: { children: React.ReactNode }) {
  return <AnimateNavigation>{children}</AnimateNavigation>;
}
